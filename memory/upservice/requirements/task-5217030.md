---
slug: task-5217030
title: "[Story] (Доработка) - Актуализировать логику работы ИИ-сторудника с лидами из-за добавления нескольких контактов"
product: upservice
task_id: 5217030
status: draft
source_task: upservice
source_design: none
fetched_at: 2026-08-24T17:22:00+03:00
figma_urls: none
entities: none
---

ИИ-сотрудник должен быть приведён в соответствие с новой моделью CRM-лида: у лида может быть несколько контактов. Цель — корректная работа ИИ-сотрудника с разделом CRM. Конкретное целевое поведение (какой контакт считается основным для ИИ, как выбирается чат, как ИИ адресует нескольких контактов) в снимке задачи не задано.

## Testable

- none

## Gaps

- В снимке нет Figma-URL → `source_design: none`, `figma_urls: none`; экраны CRM, карточки лида и диалога с ИИ-сотрудником не выдумывать.
- Снимок содержит только user story (актуализировать логику ИИ-сотрудника из-за нескольких контактов на лиде) без критериев приёмки, сценариев, правил выбора контакта/чата и ожидаемых ответов ИИ. Проверяемые пункты с `→` из текста не извлекаются без выдумывания логики.
- Связанный контекст спринта (несколько контактов на лиде) есть в других тикетах, например [task-5106553](../../requirements/task-5106553.md), но это не АС данной задачи: не переносить туда правила «первый контакт в списке» / переключение чата как требования к ИИ без явного указания в 5217030.
- В `entities/` нет карточек CRM-лида, контакта и ИИ-сотрудника. Не вводить такие slug. `entities/tasks.md` — задачи Upservice (`kind` task/meeting/ticket и т.д.), не CRM-лид. `entities/employees.md` и `entities/channels.md` — `map` only, без идентификации ИИ-сотрудника и привязки к лиду. `entities: none`.
- В `catalog/api.md` нет ресурсов CRM / leads / contacts / AI-сотрудника. Публичный каталог: files, channels, employees, projects, sprints, tags, tasks, directories. Не выдумывать эндпоинты работы ИИ с лидом или multi-contact. Связанные пути каталога только косвенно для чата: `GET|POST /v1/channels/messages`, `POST /v1/channels/messages/{channel_id}/chat/{room_uuid}/`, `POST /v1/channels/messages/{channel_unique_identifier}/`, `GET /v1/channels/messages/{channel_unique_identifier}/chat/{room_uuid}/`, `GET /v1/employees`. Проверка UI-only / API-unknown.
- Опечатка в заголовке тикета («ИИ-сторудника») сохранена в `title` как в источнике.

Did not write to Upservice.
