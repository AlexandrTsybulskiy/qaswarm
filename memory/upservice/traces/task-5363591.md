---
slug: task-5363591
title: CRM kanban sort «По приоритетам» / sales_priority
product: upservice
task_id: 5363591
requirement: requirements/task-5363591.md
status: ready
fetched_at: 2026-09-17T12:59:59+03:00
source: code
---

## Summary

Frontend on `develop` (tip `af83ebd8e`) already includes CRM ordering option `sales_priority` behind GrowthBook flag `crm-lead-priority-sorting`; remote `origin/feature/5363591-lead-priority-sorting` is fully merged (0 commits ahead of develop; merge `f96dcbf92` / feat `930df132b`). Backend `develop` (tip `d26b1b015`) has no `sales_priority` ordering yet; implementation lives only on `origin/feature/5363591-priority-sorting-on-kanban-board` (~28 commits ahead): API `ordering=sales_priority`, signal model levels 1–3, level-4 `task_info.marketing_score`, workspace gate `BUSEL_CAPITAL_WS_ID`.

## Backend

Searched primarily on `origin/feature/5363591-priority-sorting-on-kanban-board` (not present on `develop`).

- `backend:apps/tasks/orderings.py:12` — `TaskOrderingFilter.ordering_fields` adds `sales_priority`; `ordering=sales_priority` annotates levels 1–4 and orders by level, earliest active signal `occurred_at`, then `marketing_score` desc (level 4 only), then `created_at`; supports `-sales_priority` reverse
- `backend:apps/tasks/models/lead_sales_priority_signal.py:10` — model `ContactSalesPrioritySignal` with types `missed_incoming_call` / `requested_next_step` / `expressed_interest` (levels 1–3)
- `backend:apps/tasks/migrations/0179_lead_sales_priority_fields.py:1` — creates signal model + `TaskInfo.marketing_score`
- `backend:apps/tasks/models/task_info.py:90` — `marketing_score` IntegerField (level-4 score; null treated as 0 in ordering)
- `backend:apps/tasks/usecases/leads/lead_sales_priority.py:44` — `LeadSalesPriorityUseCase.process_chat_message`: missed-call via policy; else LLM classify levels 2–3; **early return unless** `chat.channel.workspace_id == settings.BUSEL_CAPITAL_WS_ID`
- `backend:apps/tasks/services/leads/lead_sales_priority.py:12` — persist/clear active signals by `source_uuid`
- `backend:apps/tasks/schemas/lead_sales_priority.py:17` — LLM JSON: `signal_type` ∈ `requested_next_step`|`expressed_interest`|null + `signal_message_uuid`
- `backend:appservice/settings/promts/lead_sales_priority_prompt.py:1` — prompt rules for level 2 (contract/KP/demo/invoice/connection terms/schedule call), level 3 interest phrases, exclude «Спасибо, посмотрим»
- `backend:apps/contacts/services/missed_calls.py:21` — `MissedCallStatePolicy`: telephony `call_missed=True` → MARK_MISSED (level 1); handled call / operator reply → CLEAR_MISSED
- `backend:apps/channels/tasks.py:1144` — on new chat messages in Busel workspace, Celery `process_lead_sales_priority_chat_message.delay(...)`
- `backend:apps/tasks/tasks.py` — Celery task delegates to usecase; lead score breakdown path also copies `marketing_score` for Busel leads
- `backend:apps/contacts/usecases/contact_scoring.py` — `refresh_lead_marketing_score` / `_update_lead_marketing_score` writes `task_info.marketing_score` from contact scoring aggregate
- `backend:apps/tasks/services/tasks/create_task/create_task_lead.py` / `update_task_lead.py` — refresh marketing score on lead create / contact change
- `backend:apps/tasks/tests/test_lead_sales_priority.py:1` — unit coverage for signals, ordering, reverse, level vs score tie-break
- `develop` — `TaskOrderingFilter.ordering_fields` has no `sales_priority` (confirm: git grep empty)

Level mapping (feature branch): **1** = active `missed_incoming_call`; **2** = `requested_next_step`; **3** = `expressed_interest`; **4** = none of the above → sort by `marketing_score` desc, then `created_at` asc. Highest level wins when multiple signals exist. Within 1–3, earlier `occurred_at` ranks higher; score is not a tiebreaker for 1–3.

## Frontend

On `develop` (feature branch fully merged; tip of feature = merge-base `930df132b`).

- `frontend:src/constants/crm.ts:7` — `CRM_ORDERING_SALES_PRIORITY = 'sales_priority'`; action title `PrioritySorting` (ns `Tasks`); `DEFAULT_CRM_ORDERING` remains `created_at`
- `frontend:src/constants/feature-flags.js:36` — GrowthBook flag id `crm-lead-priority-sorting`
- `frontend:src/hooks/crm/useIsLeadSalesPrioritySortingEnabled.ts:5` — `useFeatureIsOn(CRM_LEAD_PRIORITY_SORTING_FEATURE_FLAG)`
- `frontend:src/components/crm-view/views/CrmView/components/CrmActionsOrdering/CrmActionsOrdering.tsx:46` — kanban sort menu; hides/disables priority option when flag off
- `frontend:src/components/crm-view/views/CrmView/components/CrmViewBoard/CrmViewBoard.tsx:48` — resolves ordering key for column fetches (fallback to default if flag off)
- `frontend:src/components/crm-view/views/CrmView/hooks/useCrmViewInit.ts:83` — resets illegal `sales_priority` selection when flag off
- `frontend:src/components/crm-view/views/CrmView/utils/buildCrmLeadsFetchParams.ts:173` — passes `ordering: orderingKey` to leads list request
- `frontend:src/components/crm-view/views/CrmView/hooks/useCrmViewBoardLeads.ts:138` — `apiV1.tasks.fetchWithoutCancelling` with built params (internal tasks list)
- `frontend:src/components/contacts-view/views/contractor/leads/LeadsHeader.tsx:38` / `useFetchCompanyLeads.ts:30` — same ordering option on company leads list

Branch compare: `git log develop..origin/feature/5363591-lead-priority-sorting` empty; develop is 30 commits ahead of the feature tip with no unique FE priority diffs left on the branch.

## API alignment

- Public `catalog/api.md` lists `/v1/tasks` GET but does not document CRM kanban `ordering=sales_priority` or sales-priority signals.
- FE sends query param `ordering` with value `sales_priority` (or `-sales_priority`) via internal `apiV1.tasks` list used by CRM board columns; BE feature branch accepts it on `TaskOrderingFilter`.
- Signal ingestion is not a public CRUD API — Celery + chat history + OpenAI; score via existing contact scoring → `task_info.marketing_score`.

## Gaps

- Backend not on `develop` — e2e against staging/develop API will not sort by priority until feature branch is merged/deployed.
- FE option visible only when GrowthBook `crm-lead-priority-sorting` is on; without flag UI falls back to `created_at`.
- Signal processing and marketing score updates are gated to workspace `BUSEL_CAPITAL_WS_ID` (settings default `8`) — other workspaces may show UI (if flag on) but get empty/incorrect priority data.
- Levels 2–3 depend on async LLM classification (non-deterministic for phrase fixtures); «Спасибо, посмотрим» is prompt guidance, not a hard keyword filter in code.
- Level 1 depends on telephony `call_missed` / missed-call policy — hard to seed in pure UI e2e without API fixtures or DB signals.
- i18n string for `PrioritySorting` / Russian «По приоритетам» not found in checked-out frontend tree (likely localization submodule); verify label at runtime.
- Pagination / infinite scroll interaction with priority order not specially handled beyond server `ordering`.
- Related missed-call work also lives on the same BE feature branch history (`#5372904` commits) — overlapping with task-5372904 indicator story.

Did not modify product repositories.
