---
slug: task-4985422
title: Создание лида из тикета
product: upservice
task_id: 4985422
requirement: requirements/task-4985422.md
status: ready
fetched_at: 2026-09-01T19:15:00+03:00
source: code
---

## Repo sync

Code pull выполнен координатором до анализа; `code_roots`: frontend `(ok)`, backend `(ok)`.

## Summary

Создание лида из тикета в основном реализовано на фронтенде: в карточке тикета в работе есть меню «Добавить» с пунктом «Лид», открывающим `LeadCreatorDrawer` с предзаполнением полей из тикета. Бэкенд создаёт лид через общий `POST /v1/tasks` с `kind=lead` и обязательным `pipeline`. Расхождение с тикетом: поле «Воронка» сейчас показывается и из тикета, и из CRM (в тикете требуется показывать, из CRM — скрывать); явного флага контекста в форме не найдено.

## Task coverage

| Theme | Status | Notes |
|-------|--------|-------|
| Пункт «Лид» в меню добавления сущностей тикета | implemented | `TicketFooterActions` → `ENTITY_ACTIONS` + `ActionsDropdown` |
| Открытие дровера создания лида по клику «Лид» | implemented | `setVisibleDrawer(LEAD_CREATOR_DRAWER, data)` |
| Поле «Воронка» в дровере из тикета | partial | `MainForm.renderPipeline()` всегда рендерится; из тикета pipeline в `data` не передаётся |
| Скрытие «Воронки» в дровере из CRM | missing / partial | `Crm.tsx` открывает тот же `LeadCreatorDrawer` без флага; pipeline всё равно виден |
| Предзаполнение названия, описания, контакта, ответственного, меток | partial | title = ФИО контакта, не `ticket.title`; остальные поля прокидываются |
| Обязательное поле «Воронка» со списком воронок воркспейса | implemented | `FormPipelineSelect` + `validateRequired`; список через `fetchCrmPipelines` |
| Дровер редактирования лида без изменений | implemented | `LeadEditDrawer` использует `MainForm` с `isEdit`; отдельная story |

## Backend

- `backend:apps/tasks/serializers/task_serializer.py:298` — для `kind=lead` валидирует обязательный `pipeline` и `pipeline_status`
- `backend:apps/tasks/usecases/tasks/create_task.py:111` — создание задачи/лида с `pipeline` и `pipeline_status`
- `backend:apps/pipelines/viewsets/pipeline_viewset.py` — CRUD воронок воркспейса
- `backend:apps/tasks/tests/test_task_lead.py` — API-тесты создания и обновления лидов

## Frontend

- `frontend:src/components/tickets-view/drawers/TicketDetailsDrawer/TicketFooterActions/TicketFooterActions.jsx:43` — `ENTITY_ACTIONS` с `TYPE_LEAD` («AddLeadHeadingBtn»)
- `frontend:src/components/tickets-view/drawers/TicketDetailsDrawer/TicketFooterActions/TicketFooterActions.jsx:158` — открытие `LEAD_CREATOR_DRAWER` с title, description, responsible, contacts, tags
- `frontend:src/components/crm-view/views/CrmView/components/drawers/LeadCreatorDrawer/LeadCreatorDrawer.tsx:37` — дровер создания лида; prefill pipeline из `getCrmSelectedPipeline`
- `frontend:src/components/crm-view/views/CrmView/components/drawers/LeadCreatorDrawer/MainForm/MainForm.tsx:395` — `FormPipelineSelect` с `validateRequired(true)`
- `frontend:src/components/common/layout/Header/components/ViewInfo/info/Crm.tsx:19` — кнопка «Добавить» в CRM открывает тот же дровер с `data: {}`
- `frontend:src/components/common/controls/CustomSelect/variants/PipelineSelect/PipelineSelect.tsx:29` — загрузка списка воронок через `fetchCrmPipelines`
- `frontend:src/store/crm/actions.ts:183` — `createLead` → `apiV1.tasks.create(workspaceId, lead)`

## API alignment

- `catalog/api.md` содержит `POST /v1/tasks` для создания задач; отдельных путей CRM/pipelines в каталоге нет.
- Фронтенд создаёт лид через `POST /v1/tasks` с `kind: lead`, `pipeline`, `taskInfo.contactIds`, `responsible`, `description` — согласуется с `task_serializer` для `KIND_LEAD`.
- Список воронок: фронтенд `fetchCrmPipelines` → backend `apps/pipelines` (в публичном `catalog/api.md` не задокументировано).

## Gaps

- Figma MCP: нет доступа к файлу (403); UI сверялся по коду, не по макету.
- Не найден флаг `isFromTicket` / `showPipeline` для условного скрытия «Воронки» в CRM-контексте.
- Пункт «Лид» доступен только при статусах тикета `in_work` / `pending`; в критериях ограничение не указано.
- В форме также обязателен «Статус лида» (`FormPipelineStatusSelect`); в критериях приёмки не упомянут.
- Маппинг «Название»: код подставляет ФИО контакта, а не заголовок тикета.

Did not modify product repositories.
